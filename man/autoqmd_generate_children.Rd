% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoqmd_generate_children.R
\name{autoqmd_generate_children}
\alias{autoqmd_generate_children}
\title{Generate child QMD files from a template}
\usage{
autoqmd_generate_children(..., template, child_dir, freeze = NULL)
}
\arguments{
\item{...}{Named vectors or lists of equal length, passed to
\code{knitr::knit_expand}. Each position across vectors represents
one document to generate.}

\item{template}{Path to a Quarto template (\code{.qmd}) file to expand.}

\item{child_dir}{Directory where the generated files should be written.
The directory is created if it does not exist.}

\item{freeze}{Optional string giving the name of a variable in \code{...}
whose values will be used for deterministic file names.
If provided, filenames will be of the form \code{"_qmd_<value>.qmd"},
and files will only be regenerated if the template is newer.}
}
\value{
Invisibly returns a character vector with the paths of the generated
QMD files. Files are written to disk (unless all were skipped).
}
\description{
This function generates one or more Quarto (\code{.qmd}) files from a
template, filling in variables supplied via \code{...}. Each output file is
written to a specified directory and given either a random unique name or,
if \code{freeze} is specified, a name derived from the provided variable.
}
\details{
When \code{freeze} is used, existing files are not regenerated unless the
template has been modified since their last creation. This enables
incremental regeneration for long or expensive builds.
\itemize{
\item Random names (default) are generated using 8-digit hexadecimal strings.
\item When \code{freeze} is used, existing files are not regenerated unless the
template has been modified since their last creation. This enables
incremental regeneration for long or expensive builds.
The template timestamp is checked against each corresponding file; if the
file exists and is newer, it will be skipped.
}
}
\examples{
\dontrun{
species_vec <- c("Geelgors", "Gele Kwikstaart")

# Create child document for each species
# Use freeze to keep child files with "label" names
autoqmd_generate_children(
  species = species_vec,
  label   = gsub("\\\\s", ".", tolower(species_vec)),
  template = "species_template.qmd",
  child_dir = "spec_files",
  freeze = "label"
)
}
}
\seealso{
\code{\link[=autoqmd_prepare]{autoqmd_prepare()}}
}
