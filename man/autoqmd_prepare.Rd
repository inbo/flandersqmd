% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoqmd_prepare.R
\name{autoqmd_prepare}
\alias{autoqmd_prepare}
\title{Generate child QMDs and insert them into a parent document}
\usage{
autoqmd_prepare(
  ...,
  template,
  child_dir,
  qmd_file,
  freeze = NULL,
  template_deps = NULL,
  delim = c("{{", "}}"),
  start_marker = "<!-- AUTOGENERATED-START -->",
  end_marker = "<!-- AUTOGENERATED-END -->",
  page_break = NULL,
  quiet = FALSE
)
}
\arguments{
\item{...}{Named vectors or lists of equal length, passed to
\code{knitr::knit_expand}. Each position across vectors represents
one document to generate.}

\item{template}{Path to a Quarto template (\code{.qmd}) file to expand.}

\item{child_dir}{Directory where the generated files should be written.
The directory is created if it does not exist.}

\item{qmd_file}{Path to the Quarto file to modify.}

\item{freeze}{Optional string giving the name of a variable in \code{...}
whose values will be used for deterministic file names.
If provided, filenames will be of the form \code{"<value>.qmd"},
and files will only be regenerated if the template is newer.}

\item{template_deps}{Optional character vector of file paths that are
explicit dependencies of the template (e.g. R scripts, data files).
Their modification times are included in the dependency stamp.}

\item{delim}{Character vector of length 2 giving the opening and closing
delimiters used for template variables. Passed to \code{knitr::knit_expand}.
Defaults to \code{c("{{", "}}")}, which is convenient for Quarto templates and
avoids conflicts with LaTeX or Pandoc syntax.}

\item{start_marker}{Marker string indicating the start of the autogenerated
block. Defaults to \code{"<!-- AUTOGENERATED-START -->"}.}

\item{end_marker}{Marker string indicating the end of the autogenerated
block. Defaults to \code{"<!-- AUTOGENERATED-END -->"}.}

\item{page_break}{Character string specifying a LaTeX page break to
insert between includes. Options: \code{"newpage"}, \code{"clearpage"}.
Defaults to \code{NULL} (no page breaks).}

\item{quiet}{Logical; if \code{TRUE}, suppresses messages.}
}
\value{
Invisibly returns a list with:
\describe{
\item{children}{Paths to the generated child QMD files}
\item{parent}{Path to the updated parent QMD}
\item{dir}{Directory containing child documents}
}
}
\description{
This function wraps \code{\link[=autoqmd_generate_children]{autoqmd_generate_children()}} and
\code{\link[=autoqmd_insert_includes]{autoqmd_insert_includes()}} to support typical Quarto
pre-render workflows: generating child \code{.qmd} files from a
template and inserting the corresponding include directives
into a parent QMD.
}
\details{
The function proceeds in two steps:
\enumerate{
\item \strong{Generate child documents} using
\code{\link[=autoqmd_generate_children]{autoqmd_generate_children()}}, optionally with
deterministic filenames when \code{freeze} is used.
\item \strong{Insert include directives} into the parent \code{.qmd} by replacing
the block between the markers specified by \code{start_marker} and
\code{end_marker}.
If a template is provided, dependency timestamps are included
to trigger Quarto's freeze mechanism when relevant.
}

Input files (\code{template}, \code{qmd_file}) and marker presence are validated
before generation occurs, preventing unnecessary computation.
}
\examples{
\dontrun{
my_species <- paste("Iris", levels(iris$Species))

# Create child document for each species
# Use freeze to keep child files with label names
# Include child files in report
autoqmd_prepare(
  species = my_species,
  label = gsub("\\\\s", ".", tolower(my_species)),
  template = "_species_template.qmd",
  child_dir = "child_qmd",
  qmd_file = "report.qmd",
  freeze = "label"
)
}
}
\seealso{
\code{\link[=autoqmd_generate_children]{autoqmd_generate_children()}}, \code{\link[=autoqmd_insert_includes]{autoqmd_insert_includes()}}
}
